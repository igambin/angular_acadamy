import { MessageDateFormatPipe } from './message-date-format.pipe';

describe('MessageDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new MessageDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
